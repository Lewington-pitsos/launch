{{#if quantity}}
  <label for="cart_toggle">{{quantity}} items</label>
  <input id="cart_toggle" type="checkbox">
  <div id="items">
    <ul>
      {{#each albums}}
        <li>
          <a data-id="{{id}}" href="#"></a>

          <span>{{title}} x {{quantity}}</span>
          <small>{{format_price price}}</small>
        </li>
      {{/each}}
    </ul>
    <p>${{format_price total}}</p>
  </div>
{{else}}
  <p>0 items</p>
{{/if}}
